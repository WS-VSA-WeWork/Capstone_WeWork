import { View, Text, StyleSheet } from "react-native";

const RestaurantReview = () => {
  return (
    <View>
      <Text>RestaurantBasic</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default RestaurantReview;
